# Automatic-Resume-Builder
Developed a Java-based application that automatically generates professional resumes from structured user inputs. Implemented dynamic formatting to organize personal, educational, and skill details into a structured resume layout. Packaged the application as a standalone Windows executable using Launch4j for easy deployment.
